import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jrjpharmacy.storage',
  appName: 'moms-storage-website',
  webDir: 'dist'
};

export default config;
